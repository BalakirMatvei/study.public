# 🧩 Итерируемый объект (iterable)

Ты сказал почти правильно.

**Итерируемый объект — это объект, по которому можно пройти в цикле** `for`**.**

Примеры:

- список
    
- строка
    
- словарь
    
- множество
    
- range
    
- файл

Главное: у него есть метод `__iter__()`.

# 🧩 Итератор (iterator)

**Итератор — это объект, который выдаёт элементы по одному.**

У него есть два метода:

- `__iter__()` — возвращает самого себя
    
- `__next__()` — возвращает следующий элемент
    

Когда элементы заканчиваются — выбрасывается `StopIteration`.

# 🔧 Пример, который всё объясняет
```python
numbers = [1, 2, 3]   # это итерируемый объект

it = iter(numbers)    # это итератор

next(it)  # 1
next(it)  # 2
next(it)  # 3
next(it)  # StopIteration
```

# ✔ Идеальная формулировка для собеседования

> Итерируемый объект — это объект, по которому можно пройти в цикле `for`. Итератор — это объект, который выдаёт элементы по одному через `__next__()`. Из итерируемого объекта можно получить итератор с помощью `iter()`.
