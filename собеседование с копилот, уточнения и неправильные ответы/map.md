что такое map и для чего он  используется?

`map` — это встроенная функция в Python, которая **применяет другую функцию ко всем элементам итерируемого объекта** (списка, кортежа и т.д.) и возвращает _ленивый итератор_.

Говоря проще: **map берёт функцию и прогоняет через неё каждый элемент.**

# 🔧 Пример 1: умножаем все числа на 2
```python
numbers = [1, 2, 3, 4]

result = map(lambda x: x * 2, numbers)

print(list(result))
```

Вывод:
```python
[2, 4, 6, 8]
```

# 🔧 Пример 2: превращаем строки в числа
```python
strings = ["1", "2", "3"]

result = map(int, strings)

print(list(result))
```

Вывод:
```
[1, 2, 3]
```

# 🔧 Пример 3: применяем функцию
```python
def square(x):
    return x * x

result = map(square, [1, 2, 3])
print(list(result))
```

Вывод:
```python
[1, 4, 9]
```

# 🧠 Важные особенности `map`

- Он **не возвращает список**, а даёт _итератор_ (ленивый объект). Чтобы получить список — нужно обернуть в `list()`.
    
- Работает быстрее, чем цикл `for`, потому что оптимизирован.
    
- Часто используется вместе с `lambda`.