# 🧩 Что такое JSON — простыми словами

**JSON — это текстовый формат для хранения данных.** Он выглядит почти как Python‑словарь, но это _не Python_, а обычная строка.

Пример JSON:
```
{
  "name": "Матвей",
  "age": 18,
  "is_student": true
}
```

Отличия от Python‑словаря:

- `true` / `false` вместо `True` / `False`
    
- строки **всегда** в двойных кавычках
    
- это **строка**, а не объект Python

В Python JSON нужно **преобразовать**:
```python
import json

data = json.loads('{"name": "Матвей"}')
```

Теперь `data` — обычный словарь.