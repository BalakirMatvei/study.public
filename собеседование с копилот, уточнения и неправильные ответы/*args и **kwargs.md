Что такое *args и **kwargs? Когда их используют?

## ⭐ *args

Это способ передать **произвольное количество позиционных аргументов** в функцию.

Пример:
```python
def func(*args):
    print(args)

func(1, 2, 3)
```
вывод:
```python
(1, 2, 3)
```

## ⭐ **kwargs

Это способ передать **произвольное количество именованных аргументов**.

Пример:
```python
def func(**kwargs):
    print(kwargs)

func(name="Матвей", age=18)
```
вывод:
```python
{'name': 'Матвей', 'age': 18}
```
`kwargs` — это **словарь** всех именованных аргументов.

# 📌 Когда используют *args и **kwargs

- Когда заранее неизвестно, сколько аргументов передаст пользователь
    
- Когда нужно сделать гибкий интерфейс функции
    
- Когда пишут обёртки (decorators)
    
- Когда создают универсальные API
    

# ✔ Как выглядел бы хороший ответ на собеседовании

(*args)— собирает все позиционные аргументы в кортеж. 
(**kwargs) — собирает все именованные аргументы в словарь.
 Используются, когда заранее неизвестно количество аргументов или нужно сделать гибкую функцию.




## 🎯 Разбор твоего ответа

### ✔ `*args` и `**kwargs` — идеально

Ты описал их абсолютно правильно:

- `*args` собирает **все позиционные аргументы** в кортеж
    
- `**kwargs` собирает **все именованные аргументы** в словарь
    

Это именно то, что ждут услышать.

## ❗ Что нужно уточнить про позиционные и именованные аргументы

### Позиционные аргументы

Это **аргументы, которые передаются по порядку**, без указания имени.

Пример:
```python
def f(a, b):
    ...
f(10, 20)  # 10 — позиционный, 20 — позиционный
```

Важно: Это не «числа или строки без конкретики», а **аргументы, которые определяются своим местом**.

### Именованные аргументы

Это аргументы, которые передаются **с указанием имени**.

Пример:
```python
f(a=10, b=20)
```

Ты привёл пример `name="Copilot"` — это верно.

## ✔ Идеальный ответ на собеседовании

- **Позиционные аргументы** — передаются по порядку, их значение определяется позицией.
    
- **Именованные аргументы** — передаются с указанием имени, порядок не важен.
    
- `*args` — собирает все лишние позиционные аргументы в кортеж.
    
- `**kwargs` — собирает все лишние именованные аргументы в словарь.