Что такое lambda‑функции и когда они полезны?

# 🧩 Что такое lambda‑функции на самом деле

**lambda‑функция — это анонимная функция, у которой нет имени.** Она создаётся прямо “на месте”, обычно в одну строку.

Пример:
```python
lambda x: x * 2
```

Это то же самое, что:
```python
def f(x):
    return x * 2
```

# 📌 Когда lambda полезна

Lambda используют, когда нужно передать маленькую функцию как аргумент:

### ✔ В сортировке
```python
sorted(users, key=lambda u: u.age)
```

### ✔ В фильтрации
```python
list(filter(lambda x: x > 0, numbers))
```

### ✔ В map
```python
list(map(lambda x: x * 2, numbers))
```

### ✔ В местах, где писать `def` слишком громоздко

Например, в GUI, обработчиках событий, коллбеках.

# ❗ Важно

Lambda — **не про математику**, а про **краткость и удобство**. Она может работать с чем угодно: строками, словарями, объектами.

# ✔ Как выглядел бы идеальный ответ на собеседовании

 Lambda‑функция — это короткая анонимная функция, которую создают прямо в выражении. Используется там, где нужно передать маленькую функцию как аргумент: сортировка, фильтрация, map, обработчики событий.