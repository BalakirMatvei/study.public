# 🧩 Что такое API — простыми словами

**API — это способ, с помощью которого программы общаются друг с другом.**

Если ещё проще:

API — это как меню в ресторане. Ты отправляешь запрос → получаешь ответ.

Например:

- ты отправляешь запрос на сайт погоды
    
- сайт возвращает JSON с температурой
    
- Python читает этот JSON и использует данные
    

Пример запроса в Python:

python

```python
import requests

response = requests.get("https://api.example.com/data")
print(response.json())
```

# 🧠 Почему это важно для Python‑разработчика

Почти все реальные проекты:

- получают данные из API
    
- отправляют данные в API
    
- хранят данные в JSON
    

Это базовый навык для джуна.