## 🔍 Простое объяснение замыкания

**Замыкание (closure)** — это функция, которая _помнит_ значения переменных из внешней функции, даже после того как внешняя функция уже завершилась.

То есть:

- есть внешняя функция
    
- внутри неё создаётся внутренняя
    
- внутренняя использует переменные внешней
    
- внешняя функция уже закончила работу
    
- а внутренняя всё равно помнит эти переменные
    

Это и есть замыкание.

## 🧠 Пример, который делает всё очевидным
```python
def make_multiplier(n):
    def multiply(x):
        return x * n
    return multiply
```

Использование:
```python
double = make_multiplier(2)
triple = make_multiplier(3)

double(5)  # 10
triple(5)  # 15
```

Что происходит:

- `make_multiplier(2)` вернул функцию, которая _помнит_, что `n = 2`
    
- `make_multiplier(3)` вернул функцию, которая _помнит_, что `n = 3`
    

Хотя `make_multiplier` уже давно завершилась.

## ✔ Идеальная формулировка для собеседования

> Замыкание — это функция, которая запоминает значения переменных из внешней области видимости, даже после завершения внешней функции.