```sql
select id, name, price * amount    - выводит колонки id, name и новую                                      колонку в которой будет результат
from table_name                 перемножения колонок price и amount                       
```
