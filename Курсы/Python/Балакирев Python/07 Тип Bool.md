Оператор `not()` инвертирует условие переданое в скобках
```python
x = 7

not(x % 2 == 0) # равносильно условию (x % 2 != 0)
```

Приоритет операторов(от высокого к низкому):
- `not()`
- `and`
- `or`
