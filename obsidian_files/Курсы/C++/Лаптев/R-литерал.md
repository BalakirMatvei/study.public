он же "сырой" литерал, он же необработанный (или необрабатываемый) литерал.

Это литерал, внутри которого можно не экранировать специальные символы, обозначается `R"(random_string)"`
```c++
cout << R"(This is "quotation")"

```
Вывод: `This is "quotation"`
