```python
s.upper() #возвращает новую строку со всеми символами в верхнем регистре

s.lower() #возвращает новую строку со всеми символами в нижнем регистре

s.count('ll', start_in, end_in) #считает кол-во подстрок в исходной строке, далее можно добавить индексы(как в срезе, только вместо : будет , )

s.find('ll', start_in, end_in) #возвращает индекс где первый раз встретится подстрока (индексы работают так же как в предыдущей)

s.rfind('ll', start_in, end_in) # работает так же как find, но ищет с конца строки

s.replace(old_str, new_str) #меняет старую подстроку на новую, ещё можно указать максимальное количество замен

s.isalpha() #выводит True если вся строка состоит из букв(еслив строке есть пробел, то вернется False)

s.isdigit() #выводит True если вся строка состоит из цифр(точка в вещественных числах, - в отрицательных - не числовой символ, вернется False)

s.rjust(new_len, "symbol") #дополняет строку до new_len(число) символов символами symbol(по умолчанию пробел)
				#rjust дополняет в начало строки, а ljust в конец
s.ljust(new_len, "symbol") #дополняет строку до new_len(число) символов символами symbol(по умолчанию пробел)

s.split('sep') #возвращает список с разделеной(по разделителю sep) исходной строкой (если вызвать без аргументов, то разделится по пробелу)

'sep'.join(my_list) #обратный методу .split

s.strip() #удаляет пробелы и символы переноса строки в начале и конце строки. Можно вызвать lstrip и rstrip(удалят эти символы слева и справа)
```
[[08-09 Строки]]

!!!ВСЕ МЕТОДЫ НЕ МЕНЯЮТ ИСХОДНУЮ СТРОКУ!!!  (тк строка - неизменяемый тип данных)