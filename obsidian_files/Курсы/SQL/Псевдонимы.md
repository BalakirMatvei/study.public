```sql
select count(*) as quantity
from table_name
where condition
group by column_name
order by quantity
```
Псевдоним присвоеный через ***as*** нельзя использовать в ***where и having***, но можно использовать в ***group by*** и ***order by***, тк ***where и having*** отрабатываются до ***select*** 