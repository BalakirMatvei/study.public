Объясни разницу между областями видимости переменных: **local, global, nonlocal**.
# 🧩 Что такое `nonlocal`

`nonlocal` используется **внутри вложенной функции**, чтобы изменить переменную, которая находится **внешней, но не глобальной** области видимости.

То есть:

- `local` — внутри текущей функции
    
- `nonlocal` — в ближайшей внешней функции
    
- `global` — в глобальной области

```python
def outer():
    x = 10

    def inner():
        nonlocal x
        x = 20

    inner()
    print(x)

outer()
```
вывод:
```python
20
```

Без `nonlocal` переменная `x` внутри `inner()` была бы локальной и не изменила бы `x` из `outer()`.

# ✔ Как выглядел бы идеальный ответ на собеседовании:
`local` — переменные внутри функции. 
`global` — переменные на уровне модуля, доступны везде. 
`nonlocal` — позволяет вложенной функции изменять переменные внешней функции, но не глобальные.