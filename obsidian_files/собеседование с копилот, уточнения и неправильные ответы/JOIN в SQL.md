## 🧩 Что делает JOIN в SQL — простыми словами

**JOIN объединяет строки из двух таблиц по связанному полю.**

То есть:

> Берём таблицу А Берём таблицу B И соединяем строки, где значения совпадают.

Например:

- таблица `users`
    
- таблица `orders`

Обе содержат поле `user_id`.

JOIN позволяет получить **все заказы вместе с данными пользователя**.

## 🔧 Пример
```sql
SELECT users.name, orders.amount
FROM users
JOIN orders ON users.id = orders.user_id;
```

Результат:

|name|amount|
|---|---|
|Матвей|500|
|Матвей|1200|
|Анна|300|

## 🧠 Зачем нужен JOIN

- чтобы вытянуть данные из нескольких таблиц сразу
    
- чтобы связать сущности: пользователи → заказы, товары → категории, авторы → книги
    
- чтобы не хранить всё в одной огромной таблице
    

JOIN — это основа нормальной работы с базами данных.

## 📌 Виды JOIN (кратко)

- **INNER JOIN** — только совпадающие строки
    
- **LEFT JOIN** — все строки из левой таблицы + совпадения
    
- **RIGHT JOIN** — наоборот
    
- **FULL JOIN** — всё отовсюду
