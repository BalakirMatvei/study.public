# 📌 Итог по темам, которые мы прошли

## **1. Big O**

- Показывает, как растёт время или память алгоритма при увеличении входных данных.
    
- Примеры: O(1), O(n), O(n2).
    

## **2. Итерируемые объекты и итераторы**

- **Итерируемый объект** — по нему можно пройти в `for` (список, строка, dict…).
    
- **Итератор** — объект с `__next__()`, выдаёт элементы по одному.
    
- `iter(obj)` → создаёт итератор.
    

## **3. Генераторы**

- Функции с `yield` или генераторные выражения `(x for x in ...)`.
    
- Работают лениво, экономят память.
    

## **4. Аргументы функций**

- **Позиционные** — передаются по порядку.
    
- **Именованные** — передаются по имени.
    
- `*args` — собирает позиционные аргументы в кортеж.
    
- `**kwargs` — собирает именованные аргументы в словарь.
    

## **5. Замыкания**

- Функция, которая помнит переменные внешней функции после её завершения.
    

## **6. Области видимости**

- **local** — внутри функции.
    
- **global** — на уровне модуля.
    
- **nonlocal** — переменные внешней функции (в замыканиях).
    

## **7. ООП: базовые понятия**

- **Класс** — шаблон.
    
- **Объект** — экземпляр класса.
    
- **Атрибут** — данные класса или объекта.
    
- **Метод** — функция внутри класса.
    

## **8. Наследование и композиция**

- **Наследование** — класс получает поведение родителя.
    
- **Композиция** — класс включает объект другого класса как часть себя